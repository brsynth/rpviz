# Changelog

## [6.8.0](https://github.com/brsynth/rpViz/tree/6.8.0)

- Re-extract `rpViz` as a standalone package from
  [`rptools`](https://github.com/brsynth/rptools); version aligned with the
  `rptools` suite.
- Cross-package imports now target the standalone `rplibs` package
  (including `rplibs.cobra_format`).

See `RELEASE.md` for the full pre-split release history (0.0.1 – 0.2.1).
